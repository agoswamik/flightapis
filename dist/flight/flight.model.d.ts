export declare class Flight {
    id: string;
    to: string;
    from: string;
    price: number;
    date: string;
    seats: number;
    constructor(id: string, to: string, from: string, price: number, date: string, seats: number);
}
